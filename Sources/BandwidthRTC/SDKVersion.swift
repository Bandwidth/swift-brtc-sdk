// SDK version, sourced from the VERSION file at the repo root.
// Updated automatically by CI before each build; update manually for local development.
enum SDKVersion {
    static let current = "1.0.0-ios"
}
